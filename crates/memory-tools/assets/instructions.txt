Antes de inventar nombres de relación, jerarquías de clase o axiomas nuevos para memory_reason, comprueba si ya existen: memory_search con path_prefix "ontologies" (type: ontology) para vocabulario compartido que puedes reutilizar tal cual en classes/properties, y con type: skill para procedimientos ya documentados (via skill_ingest) que puedan aplicar a la tarea. Reutilizar lo ya declarado en vez de redescubrirlo cada sesión mantiene el grafo consistente entre agentes distintos y evita que el mismo hecho quede fragmentado bajo relaciones sinónimas (depends_on vs requires vs needs) inventadas sesión a sesión.
