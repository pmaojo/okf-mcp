{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "okf-memory": {
      "type": "stdio",
      "command": "cargo",
      "args": ["run", "--quiet", "--release", "-p", "mcp-stdio"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
